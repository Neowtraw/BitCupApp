package com.codingub.bitcupapp.common

import androidx.annotation.StringRes
import com.codingub.bitcupapp.R

enum class FeaturedType(@StringRes name: Int) {
    First(R.string.search),
    Second(R.string.search)
}